# Copy to .admin_api.env and update credentials before production/public deployment.

ADMIN_API_HOST=127.0.0.1
ADMIN_API_PORT=8789

# Choose one auth mode:
# 1) Basic Auth
ADMIN_USER=admin
ADMIN_PASS=change-me

# 2) Bearer Token (optional, if set it takes precedence)
# ADMIN_TOKEN=your_token_here
